pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/AyanX0723/Multipage.git'
            }
        }

        stage('Build') {
            steps {
                bat 'echo Building website'
            }
        }

        stage('Deploy') {
            steps {
                bat 'echo Deploying website'
            }
        }
    }
}
